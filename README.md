# ТЗ для отбора на направление "Бэкенд разработка на Go" в компанию Авито

В данном репозитории представлено решение тестового задания для отбора на направления "Бэкенд разработка на Go" в Авито
(осенняя волна 2025).

## Быстрый старт

1. Склонируйте репозиторий 
```
git clone https://github.com/SmokingElk/avito-2025-autumn-intership.git
```

2. Перейдите в директорию проекта:
```
cd avito-2025-autumn-intership/
```

3. Разверните сервис
```
docker compose up -d
или
make up
```

4. Перейдите по следующему адресу:
```
http://localhost:8080/swagger/index.html
```
По нему будет доступна сваггер-документация, включающая
ручки из тз, дополненная кодами для некоторых краевых случаев, а также эндпоинтами для доп. заданий.

6. В правом верхнем углу нажмите *Authorize* и введите админский токен для доступа к защищенным ручкам:
```
Bearer very-long-random-string
```

## Архитектура решения и структура проекта

Сервис представляет из себя бэкенд-приложение на Go, которое использует бд PostgreSQL.

Бэкенд-приложение написано в соответствии с принципами DDD и содержит следующие слои:
- *domain* - определяет доменные сущности и наиболее общие бизнес-правила
- *application* - определяет бизнес-логику конкретных use cases
- *infrastructure* - содержит реализации репозиториев и клиенты баз данных
- *presentation* - определяет точки входа в сервис (REST API, gRPC и пр.)

Использованы следующие библиотеки:
- `gin` - веб фреймворк
- `zerolog` - структурный логгер с уровнями для Go
- `sqlx` - набор расширений для стандартной библиотеки sql в Go
- `gomock` - библиотека и генератор моков для тестирования
- `swaggo` - генератор сваггер-документации по описанию ручек в специальном формате в комментариях

Общая структура проекта:
```
├── 📁 .github
├── 📁 cmd
├── 📁 config - конфигурационные файлы сервиса
├── 📁 docs - swagger дока на сервис
├── 📁 internal 
│   ├── 📁 application - слой application
│   ├── 📁 di - сборка приложения
│   ├── 📁 domain - слой domain
│   ├── 📁 infrastructure - слой infrastructure
│   ├── 📁 logger - конфигурация логгера
│   └── 📁 presentation - слой presentation
├── 📁 sql - инициализирующий sql-код для бд 
├── 📁 scripts - скрипты для заполнения бд
├── 📄 docker-compose.yml
├── 📄 Dockerfile
└── 📄 Makefile - содержит команды для управления проектом
```

## Допущения, возникшие сложности и особенности реализации

В ходе анализа ТЗ я выявил ряд непонятных для меня моментов. Ниже приведена моя трактовка этих моментов:
- В какой ситуации возможна ошибка "команда уже существует", если ручка `GET /team/add` также может обновлять список пользователей? - В случае, если список пользователей для команды не изменился, будет возвращаться ошибка.
- Что если попытаться создать команду с пользователем, который уже является членом другой команды? - должна возникать ошибка,
т. к. пользователь может являться членом только одной команды (в противном случае определение команды для назначения
ревьюверов было бы неоднозначным).
- Что будет, если пользователь является ревьювером большого количества pr? - Ручка `GET /user/getReview` будет возвращать 
вплоть до `max_pr_in_resp` (параметр конфигурации) последних pr, в которых пользователь назначен ревьювером.
- В openapi можно найти поверхностное упоминание авторизации через админский токен, также видно, что этим токеном защищены
все ручки, кроме `POST /team/add`. В своем решении я добавил middleware для потенциальной интеграцией с сервисом авторизации.
Сейчас токен сравнивается с константой, которая задается параметром `ADMIN_TOKEN` в файле `.env`. 
- Что если создат команду с некоторым списком пользователей, а затем исключить часть пользователей из него? - пользователи не
будут удалены из бд, но команда для них будет не определена. В дальнейшем они могут быть включены в другую команду.
- Что если из команды будет исключен автор или ревьювер pr? - пользователь останется автором pr для после удаления из команды. 
Если пользователь являлся ревьювером, после исключения его из команды он останется ревьювером завершенных pr, 
но будет удален из списка в открытых.
- Что если в команде не хватает пользователей для того, чтобы провести переназначение? - ручка `POST /pullRequest/reassign` 
будет возвращать ошибку.

Решение имеет следующие особенности:
- Реализована ручка для получения статистики по количеству ревью на каждого пользователя, что позволяет раздавать
ачивки "Лучший ревьювер" по итогам квартала) 
- Командам в базе данных добавлен суррогатный ключ `id`, что позволит реализовать функциональность переименования 
команд в будущем. Чтобы сохранить скорость поиска команд, добавлен индекс по имени.
- Длинные запросы для получения PR со списком ревьюверов и статистики вынесены во view, чтобы не перегружать ими 
код репозиториев.
- В репозитории настроен ci pipeline для автоматического запуска тестов и линтера перед мерджем с main и develop.
- Слои *presentation* и *application* покрыты интеграционными тестами, а use cases в *application* слое - отдельно модульными. 
Репозитории подменены моками, сгенерированными библиотекой `gomock`.
- Чтобы вынести бизнес логику из слоя *infrastructure* в слой *application* используется принцип IoC: код определяющий
бизнес правила передается при вызовах методов репозиториев в коллбэках. Для тестирования этих частей кода используется
метод `DoAndReturn` в библиотеке `gomock`.
- Добавлен healthcheck для бэкенд приложения, на который есть намек в документации.
- Добавлена ручка для массовой деактивации членов команды.

## Демо набор данных

Для загрузки демонстрационного набора данных предусмотрен скрипт. Чтобы его применить, выполните следующую команду:
```
make load_test_data
```

В наборе 3 команды:

Backend:
- Edsger Dijkstra (id=`u01`) - активен
- Alan Turing (id=`u02`) - активен
- Robert Griesmer (id=`u03`) - активен
- Dennis Ritchie (id=`u04`) - активен
- Bjarne Stroustrup (id=`u05`) - не активен
- Niklaus Wirth (id=`u06`) - активен
- Guido van Rossum (id=`u07`) - активен

Frontend:
- Vincent van Gogh (id=`u10`) - активен
- Ivan Aivazovsky (id=`u11`) - активен
- Alexandr Ivanov (id=`u12`) - активен
- Le Corbusier (id=`u13`) - активен
- Edvard Munch (id=`u14`) - не активен

Analytics:
- Louis Armstrong (id=`u20`) - активен
- Duke Ellington (id=`u21`) - активен
- Frank Sinatra (id=`u22`) - не активен
- Ella Fitzgerald (id=`u23`) - не активна

Также есть несколько открытых и смердженных PR, все они открыты участниками команды бэкенда

## Описание конфигурации линтера

В проекте использован линтер golangci. Основные особенности использованной конфигурации рассмотрены ниже:
- `timeout: 3m` - запретить анализ дольше 3-х минут
- `modules-download-mode: readonly` - запрет на модификацию go.mod во время работы линта
- `max-issues-per-linter: 0` и `max-same-issues: 0` - отключить лимиты на количество сообщений
- `default: standard` - включить набор стандартных линтеров

Используемые дополнительные линтеры:
- `govet` - ловит ошибки в шаблонах и тэгах структур
- `staticcheck` - ловит недостижимый код
- `ineffassign` - ловит бессмысленные присваивания
- `misspell` - ловит орфографические ошибки в нейминге 
- `errcheck` - ловит пропущенную обработку ошибок
- `gocyclo` - запрещает написание слишком сложных функций

Папки/файла, для которых не выполняется линтинг:
- `^vendor/` - установленные внешние зависимости (в этом проекте не используется)
- `docs.go` - файл, который генерируется swaggo для сваггер-документации
- `^mocks/` - моки (генерируются gomock)
- `.*\\.pb\\.go$` - исключение сгенерированного кода для protobuf (на случае интеграции с auth-service по gRPC)
- `.*_generated\\.go$` и `.*\\.gen\\.go$` - исключение вероятно сгенерированных go-файлов
